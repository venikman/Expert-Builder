##
# Public (client) env vars for Rsbuild must be prefixed with PUBLIC_.
#
# PostHog analytics is disabled unless PUBLIC_POSTHOG_KEY is set.
#

# Your PostHog project API key (Project Settings → API Keys)
PUBLIC_POSTHOG_KEY=

# Optional: PostHog ingestion host (e.g. https://app.posthog.com, https://us.i.posthog.com, https://eu.i.posthog.com)
PUBLIC_POSTHOG_HOST=

# Optional: "true" enables PostHog debug logging in dev
PUBLIC_POSTHOG_DEBUG=

# Optional: "true" enables autocapture (off by default to avoid capturing editor content)
PUBLIC_POSTHOG_AUTOCAPTURE=

##
# Server-side PostHog (optional)
#
# Enables backend events for Roslyn runner + API timings. If unset, server events are disabled.
#
# NOTE: This can be the same value as PUBLIC_POSTHOG_KEY.
POSTHOG_API_KEY=

# Optional: defaults to PUBLIC_POSTHOG_HOST, then https://app.posthog.com
POSTHOG_HOST=

##
# PostHog dashboards (optional)
#
# Used by `bun run posthog:dashboards` to create standard dashboards/insights via REST API.
# Create a Personal API key with scopes: dashboard:write, insight:write
#
POSTHOG_PERSONAL_API_KEY=

# PostHog Project ID (visible in the URL: /project/<id>/)
POSTHOG_PROJECT_ID=

# Optional: PostHog web app host (e.g. https://app.posthog.com, https://us.posthog.com)
POSTHOG_API_HOST=
